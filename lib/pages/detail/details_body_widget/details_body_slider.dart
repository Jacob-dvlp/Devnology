import 'package:flutter/material.dart';

class DetailsBodySlider extends StatelessWidget {
  const DetailsBodySlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // List<String> slider = [AppImage.pc1, AppImage.pc1];

    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
